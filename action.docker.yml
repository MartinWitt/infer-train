
name: 'Infer-Analyze'
description: 'Analyzes the repository with Facebook(Meta) Infer'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/martinwitt/infer-train:dev'