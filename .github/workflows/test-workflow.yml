name: container test

on:
  push:
    branches: ["*"] # Run on all branches"
  pull_request:
    branches: ["*"] # Run on all branches"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name : run test action
          uses: MartinWitt/infer-train@dev
